{
  "name": "web-server",
  "version": "1.0.0",
  "description": "web server which belongs to the computer networks project, here the data will be presented.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "group 7",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "http": "^0.0.1-security",
    "net": "^1.0.2",
    "nodemon": "^2.0.20",
    "socket.io": "^4.5.4"
  }
}
